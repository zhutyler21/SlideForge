# 请你结合文档内容，创作高质量的画图提示词
## workflow
- 首先仔细阅读文档，并根据文档整理详细的大纲（40页ppt）。
- 然后逐页生成非常详细的、顶级MMB风格和水准、审美的ppt画图提示词，背景色为#F3F4EF，内容保持专业的Science学术风格，尽可能保留原文中的内容；除背景色外的元素以黑白灰作为主色调，使用蓝色系作为唯一的强调色，使用SCQA的逻辑框架来叙述，每一页必须是高密度的咨询报告风格，包含详细的图表。

## output format：
- 请把每一页ppt对应的内容，包括1、原文中的对应内容（不要删减）、2、大纲章节、3、这一页的画图提示词（英文），按照json数据格式，存放在一起。
- 所有页的上述内容，都保存在这个json文件中。
- 共性的提示词，要放到系统提示词中。

---

当前，json的生成是基于这个提示词：
```
# 请你结合文档内容，创作高质量的画图提示词
## workflow
- 首先仔细阅读文档，并根据文档整理详细的大纲（40页ppt）。
- 然后逐页生成非常详细的、顶级MMB风格和水准、审美的ppt画图提示词，背景色为白色，内容保持专业的Science学术风格，尽可能保留原文中的内容；除背景色外的元素以黑白灰作为主色调，使用蓝色系作为唯一的强调色，使用SCQA的逻辑框架来叙述，每一页必须是高密度的咨询报告风格，包含详细的图表。

## output format：
- 请把每一页ppt对应的内容，包括1、原文中的对应内容（不要删减）、2、大纲章节、3、这一页的画图提示词（英文），按照json数据格式，存放在一起。
- 所有页的上述内容，都保存在这个json文件中。
- 共性的提示词，要放到系统提示词中。
```

我希望将这个任务的workflow改造成：
- 使用OpenAI大模型api的方式，来对json切分出来的content，来进行Slide-prompt的创作。
- 这样，就必须有一个创作slide-prompt的提示词（meta-prompt），我希望严格按照我的设计和内容要求生成。以便将来我修改这个meta-prompt，生成不同风格的ppt图片。
- 关于生成slide-prompt，我希望你写一个python，创作meta-prompt的模型名称，我已经添加到.env中了，环境变量名叫GEN_PROMPT_MODEL。
- 由于生成图片的时候，用generate_images.py，是加载了json中的system-prompt和Slide-prompt，所以也请确保逻辑正确，同样严格安装我的设计和内容要求执行。
- 请你一步一步完成工作。